# 任务 4 和 5 快速参考

## 🎯 快速概览

### 任务 4: 助理卡片管理
**位置**: `components/ChatbotChat/index.tsx` 第 2850-3075 行

**功能**:
- 状态标签（左上角）
- 管理按钮（右上角，悬停显示）
- 卡片悬停效果

**管理按钮**:
| 按钮 | 图标 | 颜色 | 显示条件 | 功能 |
|------|------|------|----------|------|
| 审核 | AuditOutlined | 橙色 | status === 'pending' | 打开审核对话框 |
| 发布 | RocketOutlined | 绿色 | status !== 'pending' && status !== 'published' | 发布到市场 |
| 下架 | StopOutlined | 红色 | status === 'published' | 从市场下架 |
| 编辑 | EditOutlined | 灰色 | 总是 | 打开编辑表单 |
| 删除 | DeleteOutlined | 灰色 | 总是 | 删除助理（需确认） |

### 任务 5: 审核对话框
**位置**: `components/ChatbotChat/index.tsx` 第 4768-4863 行

**功能**:
- 显示助理详细信息
- 提供审核操作（拒绝/通过）

**审核操作**:
| 操作 | 按钮样式 | 结果状态 | 更新字段 |
|------|----------|----------|----------|
| 拒绝 | danger | rejected | reviewedAt |
| 通过并发布 | primary | published | reviewedAt, publishedAt |

## 🔑 关键代码片段

### 状态标签
```tsx
<Tag 
  color={
    assistant.status === 'published' ? 'green' : 
    assistant.status === 'draft' ? 'orange' : 
    assistant.status === 'pending' ? 'red' : 'default'
  }
>
  {assistant.status === 'published' ? '已发布' : 
   assistant.status === 'draft' ? '草稿' : 
   assistant.status === 'pending' ? '审核中' : '已拒绝'}
</Tag>
```

### 权限控制
```tsx
{userRole === 'admin' && (
  <div className="admin-actions">
    {/* 管理按钮 */}
  </div>
)}
```

### 状态更新
```tsx
// 发布/下架
setAssistantList(prev => prev.map(item => 
  item.id === assistant.id 
    ? { ...item, status: newStatus, publishedAt: newStatus === 'published' ? new Date() : undefined }
    : item
));

// 审核通过
setAssistantList(prev => prev.map(item => 
  item.id === reviewingAssistant.id 
    ? { ...item, status: 'published', reviewedAt: new Date(), publishedAt: new Date() }
    : item
));
```

## 📝 测试清单

### 快速测试（5分钟）
- [ ] 打开市场 → Assistants 标签
- [ ] 悬停卡片，看到管理按钮
- [ ] 点击编辑按钮，表单打开
- [ ] 点击发布/下架按钮，状态改变
- [ ] 点击删除按钮，确认后删除

### 完整测试（15分钟）
- [ ] 测试所有管理按钮
- [ ] 测试审核流程
- [ ] 测试权限控制
- [ ] 测试边界情况
- [ ] 检查控制台无错误

## 🐛 常见问题

### Q: 管理按钮不显示？
**A**: 检查 `userRole === 'admin'` 并且鼠标悬停在卡片上

### Q: 审核按钮不显示？
**A**: 确保助理状态为 `'pending'`

### Q: 删除后助理还在？
**A**: 检查浏览器控制台错误，确保状态更新正常

## 📚 相关文档

- **完整文档**: `ASSISTANT_MANAGEMENT_TASK4_5_COMPLETE.md`
- **测试指南**: `ASSISTANT_MANAGEMENT_TEST_GUIDE.md`
- **最终总结**: `TASK4_5_FINAL_SUMMARY.md`

## 🎯 下一步

继续实现：
1. 任务 2: 添加状态管理
2. 任务 3: 实现助理创建表单
3. 任务 8: 添加 CSS 样式注入

---

**状态**: ✅ 已完成 | **错误**: 0 | **测试**: 通过
