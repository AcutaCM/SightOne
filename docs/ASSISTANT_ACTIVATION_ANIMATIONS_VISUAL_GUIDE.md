# 助手激活动画 - 视觉指南

## 📖 概述

本文档提供助手激活功能中所有动画效果的视觉说明和实现细节。

---

## 🎬 动画效果展示

### 1. 高亮脉冲动画 (Highlight Pulse)

**用途**: 标识新添加的助手

**视觉效果**:
```
┌─────────────────────────────────┐
│  [助手卡片]                      │  ← 初始状态
│  背景: 透明                      │
└─────────────────────────────────┘

        ↓ 动画开始

┌─────────────────────────────────┐
│  [助手卡片]                      │  ← 第1次脉冲 (0.5s)
│  背景: 浅蓝色 (15% 透明度)       │
│  外发光: 8px 蓝色光晕            │
│  缩放: 102%                      │
└─────────────────────────────────┘

        ↓ 重复 3 次

┌─────────────────────────────────┐
│  [助手卡片]                      │  ← 动画结束
│  背景: 透明                      │
└─────────────────────────────────┘
```

**时间轴**:
```
0s    0.5s   1s    1.5s   2s    2.5s   3s
|-----|-----|-----|-----|-----|-----|
 脉冲1      脉冲2      脉冲3      结束
```

**CSS 实现**:
```css
.highlighted {
  animation: highlight-pulse 1s ease-in-out 3;
}

@keyframes highlight-pulse {
  0%, 100% { 
    background: hsl(var(--heroui-primary) / 0.05);
    transform: scale(1);
  }
  50% { 
    background: hsl(var(--heroui-primary) / 0.15);
    transform: scale(1.02);
  }
}
```

---

### 2. 涟漪效果动画 (Ripple Effect)

**用途**: 按钮点击反馈

**视觉效果**:
```
点击前:
┌─────────────────────┐
│  使用该助手进行聊天  │
└─────────────────────┘

点击时 (0s):
┌─────────────────────┐
│  使用该助手进行聊天  │
│         ●           │  ← 点击位置出现小圆点
└─────────────────────┘

点击后 (0.3s):
┌─────────────────────┐
│  使用该助手进行聊天  │
│      ○○○○○         │  ← 圆形波纹扩散
└─────────────────────┘

点击后 (0.6s):
┌─────────────────────┐
│  使用该助手进行聊天  │
│   ○○○○○○○○○       │  ← 波纹完全扩散并淡出
└─────────────────────┘
```

**CSS 实现**:
```css
.activation-button::before {
  content: '';
  position: absolute;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  transition: width 0.6s ease-out, height 0.6s ease-out;
}

.activation-button:active::before {
  width: 300px;
  height: 300px;
}
```

---

### 3. 悬停提升动画 (Hover Elevation)

**用途**: 按钮和卡片的悬停反馈

**视觉效果**:
```
正常状态:
┌─────────────────────┐
│  使用该助手进行聊天  │  ← 基准线
└─────────────────────┘
        阴影: 无

悬停状态:
┌─────────────────────┐
│  使用该助手进行聊天  │  ← 向上移动 2px
└─────────────────────┘
   ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓   ← 阴影增强
```

**动画曲线**:
```
位置
 ↑
2px |     ╱────────
    |    ╱
0px |───╱
    └─────────────→ 时间
    0ms      200ms
```

**CSS 实现**:
```css
.activation-button:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px hsl(var(--heroui-primary) / 0.3);
  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

### 4. 淡入滑动动画 (Fade In Slide)

**用途**: 新助手出现在列表中

**视觉效果**:
```
初始状态 (不可见):
     ↑
     │ -10px
     │
┌────┴────────────┐
│  [新助手]       │  ← 透明度: 0%, 缩放: 95%
└─────────────────┘

动画中 (0.2s):
     ↓
     │ -5px
     │
┌────┴────────────┐
│  [新助手]       │  ← 透明度: 50%, 缩放: 97.5%
└─────────────────┘

最终状态 (0.4s):
     ↓
     │ 0px
     │
┌────┴────────────┐
│  [新助手]       │  ← 透明度: 100%, 缩放: 100%
└─────────────────┘
```

**CSS 实现**:
```css
@keyframes fadeInSlide {
  from {
    opacity: 0;
    transform: translateY(-10px) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.user-assistant-card-new {
  animation: fadeInSlide 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

### 5. 成功弹跳动画 (Success Bounce)

**用途**: 操作成功反馈

**视觉效果**:
```
缩放变化:
105% |    ╱╲
     |   ╱  ╲
100% |──╱    ╲──
     └──────────→ 时间
     0ms  150ms  300ms
```

**视觉表现**:
```
正常 → 放大 → 正常
 ●  →  ◉  →  ●
```

**CSS 实现**:
```css
@keyframes successBounce {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.activation-button-success {
  animation: successBounce 0.3s ease-out;
}
```

---

### 6. 错误抖动动画 (Error Shake)

**用途**: 操作失败反馈

**视觉效果**:
```
水平位移:
+5px |    ╱╲
     |   ╱  ╲
 0px |──╱    ╲──
     |╱        ╲
-5px |          ╲
     └──────────→ 时间
     0ms  150ms  300ms
```

**视觉表现**:
```
中 → 左 → 右 → 中
 │  ←  →  │
```

**CSS 实现**:
```css
@keyframes errorShake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-5px); }
  75% { transform: translateX(5px); }
}

.activation-button-error {
  animation: errorShake 0.3s ease-out;
}
```

---

## 🎨 状态转换图

### 按钮状态转换

```
┌─────────────┐
│   未添加     │ ← 初始状态
│  (Primary)  │
└──────┬──────┘
       │ 点击
       ↓
┌─────────────┐
│   添加中     │ ← 加载状态
│  (Loading)  │   • 显示加载图标
└──────┬──────┘   • 按钮禁用
       │ 完成      • 涟漪动画
       ↓
┌─────────────┐
│   已添加     │ ← 成功状态
│  (Success)  │   • 绿色背景
└──────┬──────┘   • 对勾图标
       │ 点击      • 弹跳动画
       ↓
┌─────────────┐
│  操作选项    │ ← 对话框
│  • 立即聊天  │
│  • 继续浏览  │
└─────────────┘
```

### 助手卡片状态

```
┌─────────────┐
│  新添加      │ ← 刚添加 (< 5秒)
│  • 高亮动画  │   • 脉冲发光 × 3
│  • 自动滚动  │   • 背景色变化
└──────┬──────┘   • 缩放效果
       │ 3秒后
       ↓
┌─────────────┐
│  普通状态    │ ← 正常显示
│  • 无动画    │   • 悬停效果
└──────┬──────┘   • 点击交互
       │ 悬停
       ↓
┌─────────────┐
│  悬停状态    │ ← 鼠标悬停
│  • 向上移动  │   • 阴影增强
│  • 阴影效果  │
└─────────────┘
```

---

## 🌈 颜色和透明度

### 主题色 (Primary)

```
浅色模式:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 5%  ░░░░░░░░░░  高亮初始/结束
10%  ▒▒▒▒▒▒▒▒▒▒  高亮背景
15%  ▓▓▓▓▓▓▓▓▓▓  高亮峰值
30%  ████████████  按钮阴影
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

暗色模式:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10%  ░░░░░░░░░░  高亮初始/结束
15%  ▒▒▒▒▒▒▒▒▒▒  高亮背景
20%  ▓▓▓▓▓▓▓▓▓▓  高亮峰值
40%  ████████████  按钮阴影
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 成功色 (Success)

```
已添加按钮:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
100% ████████████  背景色
 90% ▓▓▓▓▓▓▓▓▓▓  悬停背景
 30% ▒▒▒▒▒▒▒▒▒▒  阴影
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ⚡ 性能优化技巧

### 1. GPU 加速

所有动画元素都使用 GPU 加速:

```css
.animated-element {
  transform: translateZ(0);
  backface-visibility: hidden;
  perspective: 1000px;
}
```

**效果**: 动画在独立的合成层上运行，不影响主线程。

### 2. 只动画 Transform 和 Opacity

```css
/* ✅ 好 - 不触发重排 */
.element {
  transform: translateY(-2px);
  opacity: 0.8;
}

/* ❌ 差 - 触发重排 */
.element {
  top: -2px;
  height: 100px;
}
```

### 3. Will-Change 提示

```css
.user-assistant-card {
  will-change: transform, opacity;
}
```

**注意**: 只在需要时使用，过度使用会消耗内存。

---

## 📱 响应式适配

### 移动端优化

```
桌面端:
┌─────────────────────────────┐
│  [按钮]  完整动画效果        │
│  • 涟漪效果                  │
│  • 悬停提升                  │
│  • 阴影效果                  │
└─────────────────────────────┘

移动端:
┌───────────────────┐
│  [按钮]  简化动画  │
│  • 点击反馈        │
│  • 无悬停效果      │
│  • 轻量阴影        │
└───────────────────┘
```

### 减少动画偏好

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation: none !important;
    transition: none !important;
  }
}
```

**用户设置**: 系统设置 → 辅助功能 → 减少动画

---

## 🎯 使用场景示例

### 场景 1: 用户添加新助手

```
1. 用户点击"使用该助手进行聊天"
   └─→ 涟漪效果动画 (0.6s)
   
2. 按钮变为"添加中..."
   └─→ 加载图标旋转
   
3. 助手添加成功
   └─→ 按钮变为"已添加" + 成功弹跳 (0.3s)
   
4. 助手出现在列表顶部
   └─→ 淡入滑动动画 (0.4s)
   
5. 助手卡片高亮显示
   └─→ 脉冲动画 × 3 (3s)
   
6. 自动滚动到新助手
   └─→ 平滑滚动 (0.5s)
   
7. 3秒后高亮消失
   └─→ 淡出过渡 (0.3s)
```

**总时长**: 约 5 秒

### 场景 2: 用户悬停按钮

```
鼠标移入:
  └─→ 向上移动 2px (0.2s)
  └─→ 阴影增强 (0.2s)

鼠标移出:
  └─→ 恢复原位 (0.2s)
  └─→ 阴影减弱 (0.2s)
```

---

## 🔍 调试技巧

### Chrome DevTools

1. **查看动画时间轴**:
   - 打开 DevTools → More tools → Animations
   - 触发动画
   - 查看动画时间轴和性能

2. **检查合成层**:
   - DevTools → More tools → Layers
   - 查看哪些元素在独立层上

3. **性能分析**:
   - DevTools → Performance
   - 录制动画
   - 查看 FPS 和帧时间

### 常见问题排查

| 问题 | 可能原因 | 解决方案 |
|-----|---------|---------|
| 动画卡顿 | 未使用 GPU 加速 | 添加 `transform: translateZ(0)` |
| 动画不流畅 | 动画了 layout 属性 | 只动画 transform/opacity |
| 内存占用高 | 过度使用 will-change | 移除不必要的 will-change |
| 动画不触发 | CSS 类未正确应用 | 检查 className 逻辑 |

---

## 📚 参考资源

- [CSS Animations MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_Animations)
- [Web Animations API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Animations_API)
- [High Performance Animations](https://web.dev/animations/)
- [Cubic Bezier Generator](https://cubic-bezier.com/)

---

**最后更新**: 2024
**维护者**: Kiro AI Assistant
