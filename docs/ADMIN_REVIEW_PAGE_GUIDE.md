# 管理员审核页面使用指南

## 📋 页面概述

管理员审核页面是一个专门用于审核用户提交的助理应用的管理界面。只有管理员可以访问此页面，用于批量或单独审核助理，决定是否将其上架到商城。

## 🔗 访问路径

```
/admin/review
```

**注意**：此页面应该添加权限验证，确保只有管理员可以访问。

## ✨ 主要功能

### 1. 助理列表展示
- 以表格形式展示所有待审核的助理
- 显示助理的基本信息：图标、名称、描述、作者、标签、状态、提交时间
- 支持分页显示，默认每页 10 条

### 2. 搜索和筛选
- **搜索框**：支持按助理名称、描述或作者搜索
- **状态筛选**：可筛选全部状态、待审核、已发布、已拒绝

### 3. 单个审核
每个待审核的助理都有三个操作按钮：
- **查看**：打开详情对话框，查看完整信息
- **通过**：审核通过并上架到商城
- **拒绝**：拒绝审核

### 4. 批量审核
- 支持多选待审核的助理
- **批量通过**：一次性通过多个助理的审核
- **批量拒绝**：一次性拒绝多个助理的审核
- 显示已选择的数量

### 5. 详情查看
点击"查看"按钮打开详情对话框，显示：
- 助理图标和名称
- 当前状态
- 作者和提交时间
- 完整描述
- 标签列表
- 公开设置
- 系统提示词（可滚动查看）

在详情对话框中也可以直接进行审核操作。

## 🎯 使用流程

### 审核单个助理

1. 在列表中找到要审核的助理
2. 点击"查看"按钮查看详细信息
3. 在详情对话框中点击：
   - "通过并上架"：助理将被发布到商城
   - "拒绝"：助理将被标记为已拒绝

或者直接在列表中点击"通过"或"拒绝"按钮。

### 批量审核助理

1. 勾选要审核的助理（只能选择待审核状态的）
2. 点击顶部的"批量通过"或"批量拒绝"按钮
3. 在确认对话框中确认操作
4. 系统将批量更新所有选中助理的状态

## 📊 状态说明

| 状态 | 颜色 | 说明 |
|------|------|------|
| 待审核 | 橙色 | 用户已提交，等待管理员审核 |
| 已发布 | 绿色 | 审核通过，已上架到商城 |
| 已拒绝 | 红色 | 审核未通过 |
| 草稿 | 灰色 | 用户保存但未提交 |

## 🔐 权限控制

### 当前实现
页面本身没有权限验证，需要在路由层面添加。

### 建议实现
在 `app/admin/review/page.tsx` 中添加权限检查：

```tsx
'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';

const AdminReviewPage: React.FC = () => {
  const router = useRouter();
  
  useEffect(() => {
    // 检查用户角色
    const userRole = localStorage.getItem('userRole');
    if (userRole !== 'admin') {
      router.push('/'); // 重定向到首页
    }
  }, [router]);
  
  // ... 其余代码
};
```

或者使用中间件（推荐）：

```typescript
// middleware.ts
import { NextResponse } from 'next/server';
import type { NextRequest } from 'next/server';

export function middleware(request: NextRequest) {
  // 检查是否访问管理员页面
  if (request.nextUrl.pathname.startsWith('/admin')) {
    // 从 cookie 或 session 中获取用户角色
    const userRole = request.cookies.get('userRole')?.value;
    
    if (userRole !== 'admin') {
      return NextResponse.redirect(new URL('/', request.url));
    }
  }
  
  return NextResponse.next();
}

export const config = {
  matcher: '/admin/:path*',
};
```

## 🎨 界面特点

### 1. 响应式设计
- 适配不同屏幕尺寸
- 表格支持横向滚动

### 2. 用户体验
- 所有操作都有确认对话框，防止误操作
- 操作成功后显示提示消息
- 批量操作时显示选中数量

### 3. 数据展示
- 清晰的状态标签
- 直观的图标展示
- 完整的信息呈现

## 🔧 技术实现

### 组件结构
```
AdminReviewPage
├── Card (主容器)
│   ├── 标题栏
│   │   ├── 标题 + 待审核数量
│   │   └── 搜索框 + 状态筛选
│   ├── 批量操作栏（选中时显示）
│   └── Table (数据表格)
│       ├── 行选择
│       ├── 列定义
│       └── 分页
└── Modal (详情对话框)
    ├── 助理信息展示
    └── 操作按钮
```

### 状态管理
```typescript
const [assistantList, setAssistantList] = useState<Assistant[]>([]);
const [selectedAssistant, setSelectedAssistant] = useState<Assistant | null>(null);
const [showDetailModal, setShowDetailModal] = useState(false);
const [searchText, setSearchText] = useState('');
const [filterStatus, setFilterStatus] = useState<string>('all');
const [selectedRowKeys, setSelectedRowKeys] = useState<React.Key[]>([]);
```

### 数据过滤
```typescript
const filteredData = assistantList.filter(item => {
  const matchSearch = item.title.toLowerCase().includes(searchText.toLowerCase()) ||
                     item.desc.toLowerCase().includes(searchText.toLowerCase()) ||
                     item.author.toLowerCase().includes(searchText.toLowerCase());
  const matchStatus = filterStatus === 'all' || item.status === filterStatus;
  return matchSearch && matchStatus;
});
```

## 📝 待办事项

### 必须实现
- [ ] 添加权限验证（路由守卫或中间件）
- [ ] 连接后端 API，替换模拟数据
- [ ] 添加加载状态和错误处理
- [ ] 实现数据持久化

### 可选优化
- [ ] 添加审核历史记录
- [ ] 支持审核备注功能
- [ ] 添加导出功能（导出审核报告）
- [ ] 实现实时通知（新提交的助理）
- [ ] 添加统计图表（审核通过率等）

## 🧪 测试指南

### 功能测试
1. **访问页面**：确保只有管理员可以访问
2. **列表展示**：检查所有助理是否正确显示
3. **搜索功能**：测试搜索是否准确
4. **筛选功能**：测试状态筛选是否正常
5. **单个审核**：测试通过和拒绝功能
6. **批量审核**：测试批量操作
7. **详情查看**：检查详情对话框显示是否完整

### 边界测试
- 空数据列表
- 搜索无结果
- 全部选中后批量操作
- 快速连续点击操作按钮

## 🎯 与现有系统集成

### 1. 导航菜单
在主导航中添加管理员入口：

```tsx
{userRole === 'admin' && (
  <Menu.Item key="admin-review" icon={<AuditOutlined />}>
    <Link href="/admin/review">审核管理</Link>
  </Menu.Item>
)}
```

### 2. 数据同步
确保审核页面的数据与市场页面同步：
- 使用相同的 `assistantList` 状态
- 或者通过 Context API 共享状态
- 或者使用状态管理库（如 Redux、Zustand）

### 3. 通知系统
当有新的助理提交审核时，通知管理员：
- 在导航栏显示红点或数字徽章
- 发送系统通知
- 发送邮件提醒

## 📚 相关文档

- **任务 4 和 5 完成文档**：`ASSISTANT_MANAGEMENT_TASK4_5_COMPLETE.md`
- **测试指南**：`ASSISTANT_MANAGEMENT_TEST_GUIDE.md`
- **快速参考**：`TASK4_5_QUICK_REFERENCE.md`

## 🎉 总结

管理员审核页面提供了一个完整的助理审核管理界面，支持：
- ✅ 列表展示和详情查看
- ✅ 搜索和筛选功能
- ✅ 单个和批量审核
- ✅ 友好的用户体验
- ✅ 清晰的状态管理

下一步需要添加权限验证和后端集成，使其成为一个完整的生产级功能。
