# WebSocket状态订阅实现文档

## 概述

WebSocket状态订阅功能允许智能代理后端(3004端口)通过WebSocket实时接收来自无人机控制后端(3002端口)的状态更新，实现低延迟的状态同步。

## 功能特性

### 1. 实时状态推送
- 通过WebSocket连接接收无人机状态更新
- 支持多种消息类型（状态更新、心跳、错误等）
- 自动解析和验证消息格式

### 2. 断线重连机制
- 自动检测连接断开
- 指数退避重连策略
- 可配置的最大重连次数和延迟

### 3. 消息解析和验证
- JSON消息格式验证
- 必需字段检查
- 数据类型验证
- 错误消息处理

### 4. 心跳保活
- 定期发送心跳消息
- 保持WebSocket连接活跃
- 检测连接健康状态

## 架构设计

```
┌─────────────────────┐                    ┌─────────────────────┐
│  智能代理后端(3004)  │                    │ 无人机控制后端(3002) │
│                     │                    │                     │
│  ┌───────────────┐  │                    │  ┌───────────────┐  │
│  │ BridgeClient  │  │  WebSocket连接     │  │ WebSocket     │  │
│  │               │◄─┼────────────────────┼──│ Server        │  │
│  │ - 订阅状态    │  │                    │  │               │  │
│  │ - 接收消息    │  │  状态推送          │  │ - 推送状态    │  │
│  │ - 解析验证    │  │◄───────────────────┼──│ - 广播更新    │  │
│  │ - 自动重连    │  │                    │  │               │  │
│  └───────┬───────┘  │                    │  └───────────────┘  │
│          │          │                    │                     │
│          ▼          │                    │                     │
│  ┌───────────────┐  │                    │                     │
│  │StatusManager  │  │                    │                     │
│  │               │  │                    │                     │
│  │ - 更新状态    │  │                    │                     │
│  │ - 广播前端    │  │                    │                     │
│  └───────────────┘  │                    │                     │
└─────────────────────┘                    └─────────────────────┘
```

## 实现细节

### 1. WebSocket连接建立

```python
# 在bridge_client.py中
async def subscribe_status_updates(self, callback: Callable[[Dict[str, Any]], None]):
    """
    订阅无人机状态更新（通过WebSocket）
    
    Args:
        callback: 状态更新回调函数
    """
    if self.state != ConnectionState.CONNECTED:
        logger.error("未连接到无人机控制后端，无法订阅状态更新")
        return
    
    self.status_callback = callback
    
    # 启动WebSocket连接任务
    self.ws_task = asyncio.create_task(self._websocket_status_loop())
    logger.info("已启动WebSocket状态订阅")
```

### 2. 消息接收和处理

```python
async def _websocket_status_loop(self):
    """WebSocket状态订阅循环"""
    # 支持自动重连
    reconnect_attempts = 0
    max_reconnect_attempts = self.config.max_reconnect_attempts
    
    while reconnect_attempts < max_reconnect_attempts:
        try:
            async with self.session.ws_connect(self.ws_url) as ws:
                # 发送订阅请求
                await ws.send_json({
                    "type": "subscribe",
                    "channel": "status_updates"
                })
                
                # 接收消息循环
                async for msg in ws:
                    if msg.type == aiohttp.WSMsgType.TEXT:
                        data = json.loads(msg.data)
                        
                        # 验证消息格式
                        if self._validate_status_message(data):
                            # 处理状态更新
                            if data.get("type") == "status_update":
                                await self.status_callback(data.get("data", {}))
        
        except Exception as e:
            # 重连逻辑
            reconnect_attempts += 1
            await asyncio.sleep(delay)
```

### 3. 消息验证

```python
def _validate_status_message(self, data: Dict[str, Any]) -> bool:
    """验证状态消息格式"""
    # 检查必需字段
    if not isinstance(data, dict):
        return False
    
    if "type" not in data:
        return False
    
    # 验证不同类型消息
    if data.get("type") == "status_update":
        if "data" not in data:
            return False
        
        if not isinstance(data.get("data"), dict):
            return False
    
    return True
```

### 4. 断线重连

```python
# 指数退避重连策略
reconnect_attempts = 0
while reconnect_attempts < max_reconnect_attempts:
    try:
        # 尝试连接
        async with self.session.ws_connect(self.ws_url) as ws:
            # ... 处理消息
            pass
    
    except Exception as e:
        reconnect_attempts += 1
        
        # 计算退避延迟
        delay = min(
            self.config.reconnect_delay * (self.config.reconnect_backoff ** reconnect_attempts),
            self.config.max_reconnect_delay
        )
        
        await asyncio.sleep(delay)
```

## 消息格式

### 状态更新消息

```json
{
  "type": "status_update",
  "data": {
    "connected": true,
    "flying": false,
    "battery": 85,
    "temperature": 25,
    "height": 0,
    "speed": {"x": 0, "y": 0, "z": 0},
    "position": {"x": 0, "y": 0, "z": 0},
    "wifi_signal": 90,
    "flight_time": 0
  }
}
```

### 心跳消息

```json
{
  "type": "ping",
  "timestamp": 1699999999.999
}
```

### 心跳响应

```json
{
  "type": "pong",
  "timestamp": 1699999999.999
}
```

### 错误消息

```json
{
  "type": "error",
  "message": "错误描述",
  "code": "ERROR_CODE"
}
```

### 连接确认

```json
{
  "type": "connection_established",
  "message": "WebSocket连接已建立",
  "timestamp": "2024-01-01T00:00:00.000Z"
}
```

## 配置参数

### BridgeConfig配置

```python
@dataclass
class BridgeConfig:
    # WebSocket配置
    ws_heartbeat_interval: float = 30.0  # 心跳间隔（秒）
    
    # 重连配置
    max_reconnect_attempts: int = 5      # 最大重连次数
    reconnect_delay: float = 2.0         # 初始重连延迟（秒）
    reconnect_backoff: float = 1.5       # 指数退避因子
    max_reconnect_delay: float = 30.0    # 最大重连延迟（秒）
    
    # 超时配置
    connection_timeout: float = 10.0     # 连接超时（秒）
```

## 使用示例

### 基本使用

```python
from bridge_client import BridgeClient, BridgeConfig

# 创建配置
config = BridgeConfig(
    drone_backend_host="localhost",
    drone_backend_port=3002,
    ws_heartbeat_interval=30.0
)

# 创建桥接客户端
bridge = BridgeClient(config)

# 连接到无人机控制后端
await bridge.connect()

# 定义状态回调
async def status_callback(status_data):
    print(f"收到状态更新: {status_data}")
    print(f"电池: {status_data.get('battery')}%")
    print(f"连接: {status_data.get('connected')}")

# 订阅状态更新
await bridge.subscribe_status_updates(status_callback)

# 保持运行
await asyncio.sleep(60)

# 断开连接
await bridge.disconnect()
```

### 集成到智能代理后端

```python
# 在tello_agent_backend.py中
async def start_server(self):
    # 连接到无人机控制后端
    if self.bridge_enabled and self.bridge_client:
        connected = await self.bridge_client.connect()
        
        if connected:
            # 订阅状态更新
            async def status_callback(status_data):
                # 更新本地状态
                self.drone_status.connected = status_data.get("connected")
                self.drone_status.battery = status_data.get("battery")
                
                # 更新状态管理器
                if self.status_manager:
                    self.status_manager.update_drone_status(status_data)
                
                # 广播给前端
                await self.broadcast_status()
            
            await self.bridge_client.subscribe_status_updates(status_callback)
```

## 测试

### 运行测试脚本

```bash
# 确保无人机控制后端(3002端口)正在运行
python drone_backend.py

# 在另一个终端运行测试
python test_websocket_subscription.py
```

### 测试内容

1. **连接测试**: 验证能否连接到无人机控制后端
2. **订阅测试**: 验证WebSocket订阅是否成功启动
3. **消息接收测试**: 验证能否接收和解析状态更新
4. **消息验证测试**: 验证消息格式验证功能
5. **重连测试**: 验证断线重连机制（手动测试）

### 预期输出

```
============================================================
开始测试WebSocket状态订阅
============================================================

📝 测试1: 连接到无人机控制后端
------------------------------------------------------------
✅ 测试1通过: 成功连接到无人机控制后端

📝 测试2: 订阅WebSocket状态更新
------------------------------------------------------------
✅ 测试2通过: 已启动WebSocket状态订阅

⏳ 等待接收状态更新（30秒）...

📊 收到状态更新 #1
   连接状态: True
   飞行状态: False
   电池电量: 85%
   温度: 25°C
   高度: 0cm

📝 测试3: 验证接收到的消息
------------------------------------------------------------
✅ 测试3通过: 收到 5 个状态更新

============================================================
测试结果摘要
============================================================
   connection: ✅ 通过
   subscription: ✅ 通过
   message_parsing: ✅ 通过
   reconnection: ✅ 通过
   validation: ✅ 通过
------------------------------------------------------------
   总计: 5/5 测试通过
   状态更新接收数: 5

🎉 所有测试通过！WebSocket状态订阅功能正常
============================================================
```

## 故障排除

### 问题1: 无法建立WebSocket连接

**症状**: 日志显示"WebSocket连接错误"

**解决方案**:
1. 确认无人机控制后端(3002端口)正在运行
2. 检查防火墙设置
3. 验证WebSocket URL配置是否正确
4. 查看无人机控制后端日志

### 问题2: 未收到状态更新

**症状**: WebSocket连接成功但没有收到消息

**解决方案**:
1. 确认无人机控制后端是否推送状态
2. 检查消息格式是否匹配
3. 验证回调函数是否正确注册
4. 查看消息验证日志

### 问题3: 频繁断线重连

**症状**: 日志显示反复重连

**解决方案**:
1. 检查网络稳定性
2. 增加心跳间隔时间
3. 调整重连延迟参数
4. 检查无人机控制后端负载

### 问题4: 消息解析失败

**症状**: 日志显示"JSON解析失败"

**解决方案**:
1. 验证无人机控制后端发送的消息格式
2. 检查消息编码（应为UTF-8）
3. 查看原始消息内容
4. 更新消息格式定义

## 性能优化

### 1. 减少消息频率
- 在无人机控制后端实现状态变化检测
- 只在状态发生显著变化时推送
- 使用阈值过滤小幅度变化

### 2. 消息压缩
- 对大型消息使用压缩
- 减少网络带宽占用
- 提高传输效率

### 3. 批量处理
- 批量处理多个状态更新
- 减少回调调用次数
- 提高处理效率

### 4. 异步处理
- 使用异步回调函数
- 避免阻塞消息接收循环
- 提高并发处理能力

## 安全考虑

### 1. 消息验证
- 验证消息来源
- 检查消息完整性
- 防止恶意消息注入

### 2. 连接认证
- 实现WebSocket认证机制
- 使用令牌验证
- 限制连接来源

### 3. 错误处理
- 捕获所有异常
- 记录详细错误日志
- 防止服务崩溃

## 未来改进

1. **消息加密**: 实现端到端加密
2. **消息队列**: 添加消息缓冲队列
3. **多通道订阅**: 支持订阅多个数据通道
4. **消息过滤**: 客户端侧消息过滤
5. **性能监控**: 添加性能指标收集

## 相关文档

- [桥接客户端实现](./BRIDGE_CLIENT_IMPLEMENTATION.md)
- [状态同步实现](./STATUS_SYNC_IMPLEMENTATION.md)
- [命令转发机制](./COMMAND_FORWARDING_MECHANISM.md)
- [AI配置同步](./AI_CONFIG_SYNC_INTEGRATION.md)

## 更新日志

### v1.0.0 (2024-01-01)
- ✅ 实现WebSocket客户端连接
- ✅ 实现状态推送消息处理
- ✅ 实现断线重连机制
- ✅ 实现消息解析和验证
- ✅ 添加心跳保活机制
- ✅ 创建测试脚本
- ✅ 编写完整文档
